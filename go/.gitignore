# Generated by `make build-clean`: the register-4.13 workspace that names
# sekreto but NOT omni. Machine-specific, never committed - the same deal
# as go.work (ignored at the repo root).
/go.noomni.work
